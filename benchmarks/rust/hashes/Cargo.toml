[package]
name = "b-hashes"
version = "0.1.0"
edition = "2021"

[[bench]]
name = "bench"
path = "bench.rs"
harness = false

[dependencies]
sha2 = "0.10"

[dev-dependencies]
criterion = { version = "0.5" }
